# ---- Site ----
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ---- Supabase (v1 provider — behind src/server/adapters/supabase.ts only) ----
# From your Supabase project settings > API. Swap these (+ the adapter) for
# self-hosted Postgres/GoTrue/MinIO at scale without touching feature code.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Direct Postgres connection (for migrations / CLI / future self-hosting)
DATABASE_URL=

# ---- Email (lead delivery + saved-search alerts, Phase 3) ----
RESEND_API_KEY=
